# Address reachable from inside this Docker container.
# If Youtarr runs on the same server and uses port 3087, keep this value.
YOUTARR_URL=http://host.docker.internal:3087
YOUTARR_USERNAME=your-username
YOUTARR_PASSWORD=your-password

# Server-side storage for watch progress, feed cache, and single videos.
YOUTARR_FEED_DATA_DIR=/data

# Optional: number of seconds feed/local-video results are reused as a fast
# server-side cache. The default is 300 seconds.
# YOUTARR_FEED_CACHE_TTL_SECONDS=300

# Optional but recommended: mount the same Youtarr download folder into this
# container, using the same container path Youtarr uses.
# This lets the app stream local files directly instead of proxying via Youtarr.
YOUTARR_MEDIA_DIR=/usr/src/app/data

# Optional: when the media mount is writable, delete every local file matching
# the same YouTube video ID after a Youtarr delete succeeds. Useful for
# dual-quality copies that Youtarr may not remove itself.
# YOUTARR_FEED_DELETE_LOCAL_FILES=false

# Optional: trigger Youtarr's "rescan files on disk" before a redownload/missing
# download, then wait briefly so Youtarr clears stale missing state first.
# YOUTARR_RESCAN_BEFORE_REDOWNLOAD=true
# YOUTARR_RESCAN_BEFORE_REDOWNLOAD_TIMEOUT_SECONDS=45

# Optional: a separate API key for download requests.
# YOUTARR_API_KEY=

# Optional: ask Youtarr for a second 1080p copy when this app starts a download.
# Store the second copy separately so it does not overwrite the original.
# YOUTARR_DUAL_QUALITY_DOWNLOADS=false
# YOUTARR_PRIMARY_DOWNLOAD_RESOLUTION=
# YOUTARR_SECONDARY_DOWNLOAD_RESOLUTION=1080
# YOUTARR_SECONDARY_DOWNLOAD_SUBFOLDER=__1080p

# Optional: fallback for exact YouTube publishedAt timestamps in Youtarr Feed.
# Prefer setting the YouTube API key in Youtarr itself first. This variable is
# only needed when Youtarr still returns dates without times.
# YOUTUBE_API_KEY=

# Alternatives to username/password:
# YOUTARR_SESSION_TOKEN=
# YOUTARR_AUTH_DISABLED=false

# Optional: Apple-compatible server-side transcoding.
# "file" creates a reusable compatible MP4 cache for 1440p/4K videos.
# Set MIN_HEIGHT=0 to allow all resolutions.
# YOUTARR_TRANSCODE_ENABLED=true
# YOUTARR_TRANSCODE_ACCEL=vaapi
# YOUTARR_TRANSCODE_DEVICE=/dev/dri/renderD128
# YOUTARR_TRANSCODE_OUTPUT_MODE=file
# YOUTARR_TRANSCODE_MIN_HEIGHT=1440
# YOUTARR_TRANSCODE_VAAPI_QUALITY=20

# Optional: let this app trigger a Plex library scan after a download.
# Not needed when Youtarr already does this for you.
# PLEX_URL=http://host.docker.internal:32400
# PLEX_TOKEN=
# PLEX_LIBRARY_ID=
